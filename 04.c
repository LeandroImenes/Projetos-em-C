#include <stdio.h>
int main(){
	float sh, hm, sm, st;
	printf("Qual seu salario por hora?");
	scanf("%f", &sh);
	printf("Quantas horas voce trabalha no mes?");
	scanf("%f", &hm);
	sm = (sh * 24) * 30;
	
}